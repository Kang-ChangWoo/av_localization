# Exp. 3 — can acoustics resolve the strongest visual ambiguity?

The two hypotheses vision ranks highest, on samples where exactly one is
correct: does the acoustic evidence alone pick the right one, and does it
still on the hardest fifth by m_v? Main protocol plus the all-pairs
supplementary. Self-contained: reads `../../outputs/analysis` and
`../../feasible/results`, writes only here.

```
src/top2_disambiguation.py  -> data/top2_disambiguation.json, figs/top2_disambiguation.png,
                               the table block of md/top2_disambiguation.md
md/top2_disambiguation.md   tables (generated block) and the reading of them
```

```bash
cd analysis/exp3_top2_disambiguation && /opt/conda/envs/f3loc/bin/python src/top2_disambiguation.py
```
