# Exp. 5 — ablations

Projection source, three against four scalars, the three ways past the
visual shortlist, the one-scalar cell-product rule, and the projection
transfer matrix, laid side by side across benchmarks and backbones from the
selection runs in `../../feasible/results`. Self-contained: reads those JSON
files, writes only here.

```
src/ablation.py   -> data/ablation.json, figs/projection_source.png, the table block of md/ablation.md
md/ablation.md    five tables (generated block) and the reading of them
```

```bash
cd analysis/exp5_ablation && /opt/conda/envs/f3loc/bin/python src/ablation.py
```
