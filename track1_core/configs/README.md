# Configs

Configuration schemas will define dataset adapters, floorplan resolution, ray conventions, likelihood temperatures, fusion settings, and evaluation subsets. No machine-specific paths belong here.
