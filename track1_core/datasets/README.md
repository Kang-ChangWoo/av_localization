# Datasets

Dataset adapters will transform source-specific RGB, acoustic observations, floorplan maps, and ground-truth poses into the shared contracts. Raw data and cached artifacts are external to this repository.
