# Floorplan

Floorplan utilities will own occupancy normalization, metric/grid coordinate conversion, directional geometry caches, and valid-pose masks. They must not depend on a particular dataset annotation format.
