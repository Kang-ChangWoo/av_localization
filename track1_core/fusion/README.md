# Fusion

Fusion utilities will calibrate visual and acoustic likelihoods on the shared valid support, then combine them into a posterior. Raw likelihood scales must not silently determine modality weight.
