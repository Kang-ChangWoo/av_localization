# Likelihood

Likelihood modules will convert visual and acoustic observations into pose volumes with the shared shape `(H, W, O)`. Each module must explicitly declare ray ordering, units, field of view, and invalid-observation handling.
